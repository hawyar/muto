import { Dataset, DatasetOptions, Workflow } from "./types";
export declare function createDataset(source: string, opt: DatasetOptions): Dataset;
export declare function createWorkflow(name: string): Workflow;
